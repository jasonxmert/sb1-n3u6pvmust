// Extended database of major cities worldwide with comprehensive coverage
export const cityData = {
  // Cities data remains the same but remove the sort at the end
  // ... all city entries remain identical ...
};